@model IEnumerable<BookMate.Models.Admin>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.APassword)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.APassword)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AUserName }) |
            @Html.ActionLink("Details", "Details", new { id=item.AUserName }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AUserName })
        </td>
    </tr>
}

</table>
